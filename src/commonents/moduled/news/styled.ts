import styled from "@emotion/styled";

const AarticleTitle = styled.article`
  /* position: absolute; */
`;

const View = styled.span`
  margin: -47px 0px 0px 1198px;
  font-size: 13px;
  font-weight: 500;
  position: absolute;
`;
const Picture = styled.picture`
  margin: -57px 0px 0px 1240px;
  position: absolute;
`;
const Button = styled.button`
  all: unset;
  cursor: pointer;
`;

const Img = styled.img`
  margin: 0px 0px 0px 0px;
  width: 312px;
  height: 179px;
`;

const Article = styled.article`
  display: flex;
  flex-wrap: wrap;
  padding: 63px 0 80px 0;
`;

const Figure = styled.figure`
  width: 412px;
  height: 577px;
  background-color: white;
  margin: 0px 18px 18px 0px;
  padding: 50px;
`;

const Figure4 = styled.figure`
  width: 412px;
  height: 409px;
  background-color: white;
  margin: 0px 18px 18px 0px;
  padding: 50px;
`;

const Figcaption = styled.figcaption`
  padding: 10px 10px;
`;

const BoxTitle = styled.p`
  font-size: 20px;
  width: 260px;
  padding: 0 5px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;

const BoxContent = styled.p`
  color: #666;
  font-size: 17px;
  line-height: 140%;
`;
const BoxDay = styled.p`
  font-size: 12px;
  color: #666;
`;
export {
  AarticleTitle,
  View,
  Picture,
  Button,
  Img,
  Article,
  Figure,
  Figure4,
  Figcaption,
  BoxTitle,
  BoxContent,
  BoxDay,
};
